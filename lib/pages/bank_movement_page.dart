import 'package:flutter/material.dart';

class BankMovementPage extends StatelessWidget {
  const BankMovementPage({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text('This is the Bank Movement page.'),
    );
  }
}
